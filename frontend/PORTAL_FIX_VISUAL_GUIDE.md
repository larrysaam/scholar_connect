# Portal Race Condition - Visual Flow Diagram

## The Problem

```
┌─────────────────────────────────────────────────────────┐
│ User Action: Click Submit in Dialog with Select        │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│ 1. API Call Starts                                      │
│    await api.submit(data)                               │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│ 2. API Returns Success                                  │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│ 3. Dialog Closes IMMEDIATELY ❌                         │
│    setOpen(false)                                       │
└────────────────┬────────────────────────────────────────┘
                 │
                 ├──────────────────┬─────────────────────┐
                 │                  │                     │
                 ▼                  ▼                     ▼
     ┌──────────────────┐  ┌──────────────┐  ┌─────────────────┐
     │ React starts     │  │ Dialog       │  │ Select dropdown │
     │ unmounting       │  │ Portal       │  │ Portal cleanup  │
     │ Dialog tree      │  │ cleanup      │  │ (async)         │
     └──────┬───────────┘  └──────┬───────┘  └────────┬────────┘
            │                     │                    │
            │                     │                    │
            └─────────────────────┴────────────────────┘
                                  │
                                  ▼
                    ┌──────────────────────────────┐
                    │ ⚠️ RACE CONDITION!           │
                    │                              │
                    │ React tries to remove        │
                    │ DOM nodes that Select        │
                    │ portal is still cleaning up  │
                    │                              │
                    │ Error: removeChild on Node   │
                    │ → WHITE SCREEN CRASH 💥      │
                    └──────────────────────────────┘
```

## The Solution

```
┌─────────────────────────────────────────────────────────┐
│ User Action: Click Submit in Dialog with Select        │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│ 1. Close All Portal Components First ✅                 │
│    closeAllPortals() // Dispatch Escape key            │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│ 2. Wait for Portal Cleanup (100ms)                     │
│    await new Promise(r => setTimeout(r, 100))          │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│ 3. API Call Starts                                      │
│    await api.submit(data)                               │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│ 4. API Returns Success                                  │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│ 5. Wait for State Propagation (500ms) ✅                │
│    await new Promise(r => setTimeout(r, 500))          │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│ 6. Dialog Closes Safely ✅                              │
│    setOpen(false)                                       │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
      ┌───────────────────────────┐
      │ ✅ SMOOTH CLOSE           │
      │                           │
      │ All portals cleaned up    │
      │ No race condition         │
      │ Dialog animates out       │
      │ No errors                 │
      │ → HAPPY USER 😊           │
      └───────────────────────────┘
```

## Hook Flow Diagram

```
┌─────────────────────────────────────────────────────────┐
│ useDialogWithPortals()                                  │
│                                                         │
│ Returns:                                                │
│  • isOpen: boolean                                      │
│  • isProcessing: boolean                                │
│  • openDialog()                                         │
│  • safeClose()                                          │
│  • closeAllPortals()                                    │
│  • handleSubmitWithSafeClose()                          │
└────────────────┬────────────────────────────────────────┘
                 │
                 │
    ┌────────────┴────────────┐
    │                         │
    ▼                         ▼
┌─────────────┐        ┌──────────────┐
│ openDialog()│        │ safeClose()  │
│             │        │              │
│ Set:        │        │ 1. Set       │
│ isOpen=true │        │    processing│
│ isProcessing│        │              │
│ =false      │        │ 2. Close     │
└─────────────┘        │    portals   │
                       │              │
                       │ 3. Wait 100ms│
                       │              │
                       │ 4. Wait 500ms│
                       │              │
                       │ 5. Set       │
                       │    isOpen=   │
                       │    false     │
                       │              │
                       │ 6. Set       │
                       │    processing│
                       │    =false    │
                       └──────────────┘
```

## Component Integration Flow

```
┌──────────────────────────────────────────────────────────┐
│ Component Using Hook                                     │
└───────────────────────┬──────────────────────────────────┘
                        │
        ┌───────────────┴───────────────┐
        │                               │
        ▼                               ▼
┌───────────────┐              ┌────────────────┐
│ Dialog        │              │ Form/Content   │
│               │              │                │
│ Props:        │              │ • Select       │
│ • open=       │              │ • Input        │
│   isOpen      │              │ • Textarea     │
│               │              │                │
│ • onOpenChange│              │ Submit Button: │
│   =openDialog │              │ • disabled=    │
│               │              │   isProcessing │
│               │              │                │
│               │              │ • onClick=     │
│               │              │   handleSubmit │
└───────┬───────┘              └────────┬───────┘
        │                               │
        └───────────────┬───────────────┘
                        │
                        ▼
                ┌───────────────┐
                │ handleSubmit  │
                │               │
                │ async () => { │
                │   const ok =  │
                │   await api() │
                │               │
                │   if (ok) {   │
                │     await     │
                │     safeClose│
                │     ()        │
                │   }           │
                │ }             │
                └───────────────┘
```

## Timing Diagram

```
Time →
0ms    100ms   200ms   300ms   400ms   500ms   600ms
│      │       │       │       │       │       │
│      │       │       │       │       │       │
▼      ▼       ▼       ▼       ▼       ▼       ▼

User clicks submit
│
├─ Escape key dispatched
│  │
│  └─ Select dropdown starts closing
│     │
│     └─ (animation 50-100ms)
│
└─ Wait 100ms ──────┐
                    │
                    ├─ All portals closed ✅
                    │
                    ├─ API call starts
                    │  │
                    │  └─ (network 100-500ms)
                    │     │
                    │     └─ API success
                    │
                    └─ Wait 500ms ────────────┐
                                              │
                                              ├─ React state settled ✅
                                              │
                                              ├─ Dialog closes
                                              │  │
                                              │  └─ (animation 200ms)
                                              │     │
                                              │     └─ Complete! ✅
```

## Error vs Success Comparison

### ❌ Without Fix:
```
┌──────────┐
│ Submit   │
└────┬─────┘
     │ 0ms
     ├─ API call
     │
     │ 100ms
     ├─ Success
     │
     │ 101ms
     ├─ setOpen(false)
     │
     │ 102ms
     ├─ Dialog unmount
     ├─ Portal cleanup (async)
     │
     │ 103ms
     ├─ 💥 RACE CONDITION
     └─ removeChild error
```

### ✅ With Fix:
```
┌──────────┐
│ Submit   │
└────┬─────┘
     │ 0ms
     ├─ Close portals (Escape)
     │
     │ 100ms
     ├─ Portals closed ✅
     ├─ API call
     │
     │ 200ms
     ├─ Success
     │
     │ 700ms (200 + 500 wait)
     ├─ setOpen(false)
     │
     │ 701ms
     ├─ Dialog unmount
     │
     │ 900ms
     └─ ✅ CLEAN CLOSE
```

## State Machine Diagram

```
                    ┌──────────┐
                    │  CLOSED  │
                    └────┬─────┘
                         │
                         │ openDialog()
                         │
                         ▼
                    ┌──────────┐
                    │   OPEN   │
                    └────┬─────┘
                         │
                         │ handleSubmit()
                         │
                         ▼
                 ┌───────────────┐
                 │  PROCESSING   │
                 │ (isProcessing │
                 │   = true)     │
                 └───────┬───────┘
                         │
                ┌────────┴─────────┐
                │                  │
         success│                  │failure
                │                  │
                ▼                  ▼
         ┌────────────┐     ┌────────────┐
         │  CLOSING   │     │    OPEN    │
         │ (portals   │     │  (retry)   │
         │  cleanup)  │     └────────────┘
         └──────┬─────┘
                │
                │ safeClose()
                │ completes
                │
                ▼
           ┌──────────┐
           │  CLOSED  │
           └──────────┘
```

## Quick Reference

### Problem Signature:
```
Error: NotFoundError: Failed to execute 'removeChild' on 'Node'
Component: <Dialog> with <Select>
Trigger: Immediate close after async operation
```

### Solution Signature:
```typescript
// Import
import { useDialogWithPortals } from '@/hooks/useDialogWithPortals';

// Use
const { isOpen, isProcessing, safeClose } = useDialogWithPortals();

// Submit
const success = await api.submit();
if (success) await safeClose();

// UI
<Button disabled={isProcessing}>
  {isProcessing ? 'Saving...' : 'Submit'}
</Button>
```

### Key Timings:
- Portal close dispatch: **Immediate**
- Portal cleanup wait: **100ms**
- State propagation wait: **500ms**
- Total overhead: **~600ms**
- User perception: **Feels instant with loading indicator**

### Success Metrics:
- ✅ Zero removeChild errors
- ✅ Smooth dialog animations
- ✅ No white screen crashes
- ✅ Professional loading states
- ✅ Better user experience
